{
  "name": "verifai-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Workspace root for VerifAI - verify whether a third-party Claude/GPT API endpoint really serves the model it claims",
  "license": "MIT",
  "packageManager": "pnpm@10.22.0",
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "build": "pnpm -r --filter=!verifai-monorepo run build",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.portable.json && tsc -p packages/web/tsconfig.json",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "check": "pnpm run lint && pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
